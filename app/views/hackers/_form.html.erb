<%= form_with(model: hacker) do |form| %>
  <% if hacker.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(hacker.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% hacker.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :age, style: "display: block" %>
    <%= form.text_field :age %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
